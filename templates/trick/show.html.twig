{% extends 'base.html.twig' %}

{% block title %}Trick{% endblock %}

{% block body %}
 <div class="container">
    <h1 class="m-4">Figure en détail</h1>

    <table class="table table-striped">
        <tbody>

            <tr>
                <th>Name</th>
                <td>{{ trick.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ trick.description }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ trick.dateCreation ? trick.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ trick.cover }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trick_index')}}"  class="btn btn-outline-info">back to list</a>

    <a href="{{ path('trick_edit', {'id': trick.id})}}" class="btn btn-outline-secondary">edit</a>

    {{ include('trick/_delete_form.html.twig') }}

 </div>
{% endblock %}
